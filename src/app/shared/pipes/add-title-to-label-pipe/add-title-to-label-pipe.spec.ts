import { AddTitleToLabelPipe } from './add-title-to-label-pipe';

describe('AddTitleToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AddTitleToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
